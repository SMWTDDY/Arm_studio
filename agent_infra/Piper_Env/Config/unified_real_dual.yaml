# Unified Piper entrypoint: real dual-arm backend.

backend: real
arm_mode: dual
control_mode: joint
hz: 30
with_cameras: true
start_cameras: false

real:
  config_path: agent_infra/Piper_Env/Config/dual_piper_config.yaml
