# third_party

本目录存放为本项目保留的外部 SDK 源码快照。

当前内容：

```text
pyorbbecsdk/  Orbbec Python SDK 源码快照，用于后续安装和验证 depth SDK
```

默认真实相机链路仍使用 V4L2 RGB fallback，不要求安装 `pyorbbecsdk`。需要验证 Orbbec depth 时，再在 `SL` 环境中从这里安装或构建 SDK。

