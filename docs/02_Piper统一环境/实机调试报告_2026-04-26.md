# 实机调试报告 2026-04-26

## 1. 调试范围

本次调试目标是在 `SL` conda 环境下检查已经合并后的 Piper 统一环境，覆盖四个 CAN 机械臂接口、三个 Orbbec 相机、`pyAgxArm` 本地 SDK、统一双臂配置入口。

为避免误动作，本次只执行了只读探针、配置 dry-run、摄像头读取测试和 Python 编译检查；没有执行 `reset`、`step`、轨迹回放、采集脚本，也没有发送机械臂运动指令。

## 2. 当前环境

- 项目目录：`/home/wry/Desktop/NewArm_studio/Arm_studio`
- Conda 环境：`SL`
- 本地 SDK：`Arm_studio/pyAgxArm`
- Python 导入路径已确认指向：`/home/wry/Desktop/NewArm_studio/Arm_studio/pyAgxArm/pyAgxArm/__init__.py`

## 3. 已修改和确认的代码项

- `agent_infra/Piper_Env/Env/utils/piper_arm.py` 已优先加载项目内本地 `pyAgxArm`，避免误用 conda/site-packages 中的旧版本。
- `agent_infra/Piper_Env/Env/utils/piper_base_env.py` 已支持从 config 读取 `robot_model` 和 `firmware_version`。
- `teleop/real_to_sim.py` 已同步本地 SDK 路径、固件参数和新版夹爪字段兼容。
- `agent_infra/Piper_Env/Config/piper_config.yaml` 与 `dual_piper_config.yaml` 已将 `firmware_version` 设置为 `v183`。
- `scripts/piper/test_unified_env.py --dry-config` 已补充 real backend 相机角色解析，不启动硬件也能检查真实配置中的相机字段。

## 4. CAN 与机械臂状态

用户已手动激活四个 CAN 口，系统识别到：

```text
can_mr -> USB 1-4.2:1.0
can_sr -> USB 1-4.3:1.0
can_sl -> USB 1-9.3:1.0
can_ml -> USB 1-9.2:1.0
```

`ip -details -statistics link show` 检查结果：

- `can_mr`、`can_sr`、`can_sl`、`can_ml` 都处于 `UP`。
- 四个 CAN 均为 `ERROR-ACTIVE`。
- 波特率均为 `1000000`。
- 未看到 bus-off 状态。

只读 SDK 探针结果：

- `can_mr` 可以完整读取固件、关节角、末端位姿、机械臂状态和夹爪状态。
- `can_mr` 固件为 `S-V1.8-7`，因此当前应使用 `firmware_version: "v183"`。
- `can_mr` 当前状态为 `STANDBY`、`NORMAL`，未报告关节限位或通信错误。
- `can_ml` 能读取到固件信息，但暂未读到完整关节状态和末端位姿。
- `can_sr` 和 `can_sl` 在 SDK 只读探针中暂未读到状态数据。

当前判断：右主臂链路已经基本可读；左主臂、左从臂、右从臂还需要进一步确认是否处于正确上电/模式/固件通讯状态。

## 5. 相机状态

系统层识别到三个 Orbbec RGB/depth 设备组：

```text
CH2592100GJ -> RGB USB 1-4.4.3，depth USB 1-4.4.1
CH28822007C -> RGB USB 1-9.4.3，depth USB 1-9.4.1
CH27152011E -> RGB USB 1-7.3，depth USB 1-7.1
```

V4L2 节点：

```text
/dev/video0 /dev/video1 -> CH2592100GJ
/dev/video2 /dev/video3 -> CH28822007C
/dev/video4 /dev/video5 -> CH27152011E
```

普通用户下 OpenCV 无法打开 `/dev/video0` 到 `/dev/video5`。提权测试后，`/dev/video0`、`/dev/video2`、`/dev/video4` 均可读取 `640x480x3` 彩色帧，说明硬件和 V4L2 节点本身可用，主要问题是当前用户没有视频设备访问权限。

另外，当前 `SL` 环境缺少 `pyorbbecsdk`，因此 Orbbec SDK 深度接口暂不可用，只能先走 V4L2 彩色图像路径。

## 6. 统一环境检查

已执行：

```bash
PYTHONPATH=. conda run -n SL python -m py_compile \
  agent_infra/Piper_Env/Env/utils/piper_arm.py \
  agent_infra/Piper_Env/Env/utils/piper_base_env.py \
  teleop/real_to_sim.py
```

结果：通过。

已执行：

```bash
PYTHONPATH=. conda run -n SL python scripts/piper/test_unified_env.py \
  -cfg agent_infra/Piper_Env/Config/unified_real_dual.yaml \
  --dry-config
```

结果：通过。解析到的双臂字段为：

```text
state keys:
left_joint_pos, left_joint_vel, left_ee_pose, left_gripper_pos,
right_joint_pos, right_joint_vel, right_ee_pose, right_gripper_pos

action keys:
left_arm, left_gripper, right_arm, right_gripper

rgb roles:
base_camera, left_hand_camera, right_hand_camera
```

## 7. 当前阻塞点

1. 摄像头普通用户权限未配置。

当前用户 `wry` 不在 `video` 组，所以普通运行采集脚本会打不开 `/dev/video*`。建议执行：

```bash
sudo usermod -aG video wry
```

执行后需要注销重新登录，或重启电脑，新的组权限才会生效。

2. Orbbec 深度 SDK 未安装。

如果后续需要使用 Orbbec SDK 深度图，而不是 V4L2 彩色图，需要在 `SL` 环境安装并验证 `pyorbbecsdk`。

3. 三个 CAN 口还没有读到完整 SDK 状态。

`can_ml` 只读到固件，`can_sr` 和 `can_sl` 暂无 SDK 状态，需要继续确认机械臂上电、急停、模式、CAN 线序、接口映射和固件通讯。

4. 尚未进行真实环境初始化。

`PiperEnv` 初始化会设置主从臂模式和速度/加速度参数，属于写入型操作。当前还没有在四臂上执行，需要在确认安全姿态、急停可用、人员远离机械臂后再做。

5. 尚未进行真实运动闭环。

还未执行主臂控制从臂、双臂遥操、真实采集、回放和仿真同步采集。

## 8. 下一步建议

1. 先修复摄像头权限：把 `wry` 加入 `video` 组并重新登录。
2. 重新运行 V4L2 相机检查，确认普通用户可以读取 `/dev/video0`、`/dev/video2`、`/dev/video4`。
3. 对四个 CAN 口做最小只读排查，确认每个接口是否有持续 CAN 帧和 SDK 状态。
4. 在安全条件下运行真实环境初始化，但先不要 `reset` 或 `step`。
5. 单臂链路先跑通：主臂读数、从臂状态、相机帧、H5 录制。
6. 再进入双臂链路：左右主臂读数、左右从臂状态、三相机同步、统一 H5 字段。
7. 最后再测试仿真与真实脚本对应关系，确保只改 config 就能切换 `backend=real|sim` 和 `arm_mode=single|dual`。

## 9. 继续排查记录

本轮继续保持只读原则，没有创建真实 env，没有执行 `reset`、`step`、`enable`、`move_j`、`move_p` 或主从跟随。

当前登录会话检查：

```text
uid=1000(wry) gid=1000(wry) groups=1000(wry),65534(nogroup)
```

说明 `wry` 当前会话仍未获得 `video` 组权限。沙箱内 `/dev/video*` 不可见，但沙箱外确认设备节点存在：

```text
crw-rw----+ root video /dev/video0
crw-rw----+ root video /dev/video1
crw-rw----+ root video /dev/video2
crw-rw----+ root video /dev/video3
crw-rw----+ root video /dev/video4
crw-rw----+ root video /dev/video5
```

普通用户 OpenCV 读取仍失败；提权后再次确认：

```text
/dev/video0 opened=True  read=True  shape=(480, 640, 3)
/dev/video1 opened=False read=False
/dev/video2 opened=True  read=True  shape=(480, 640, 3)
/dev/video3 opened=False read=False
/dev/video4 opened=True  read=True  shape=(480, 640, 3)
/dev/video5 opened=False read=False
```

结论：三台 Orbbec 的 RGB V4L2 节点仍可用，当前采集应使用 `/dev/video0`、`/dev/video2`、`/dev/video4` 对应的三个 RGB 节点；`/dev/video1/3/5` 暂不作为 RGB 采集入口。`pyorbbecsdk` 仍未安装。

CAN 继续排查结果：

- 四个 CAN 口仍为 `UP`、`ERROR-ACTIVE`、`1000000` bitrate。
- `can_mr` 两秒 `candump` 有大量持续帧，SDK 可读取完整固件、关节角、末端位姿、状态。
- `can_sr` 两秒 `candump` 无持续帧，SDK 未读到固件或状态。
- `can_ml` 两秒 `candump` 无持续帧，本轮 SDK 未读到固件或状态。
- `can_sl` 两秒 `candump` 无持续帧，SDK 固件查询出现 `No buffer space available`，更像是适配器在线但总线侧没有正常应答。

当前更明确的判断：右主臂 `can_mr` 通讯正常；另外三路不是软件协议层已经跑通后的字段问题，而是硬件总线侧没有持续 Piper 状态帧，需要先检查对应机械臂上电、急停、CAN 线、终端/转接、接口映射和设备是否处于会广播状态的模式。
